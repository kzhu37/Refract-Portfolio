import React from 'react'
import ReactDOM from 'react-dom/client'
import CorrectionCanvas from '../overlay/CorrectionCanvas'

ReactDOM.createRoot(document.getElementById('overlay-root')!).render(<CorrectionCanvas />)
